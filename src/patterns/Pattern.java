package patterns;

/*
 * This simple enum lists all the available patterns that the user can select
 * from the menu.
 */
public enum Pattern
{
	PERSONAL,
	GLIDER,
	BLINKER,
	BLOCK,
	HERSCHEL,
	GLIDER_GUN,
}
